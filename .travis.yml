language: java
jdk:
  - oraclejdk8
sudo: required
services:
  - docker

cache:
  yarn: true
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.m2/wrapper'
    - 'client/node_modules'
    - 'client/node'

install: true
script:
  - ./mvnw package -e

after_success:
  - ./.travis-push-docker.sh